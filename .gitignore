# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Build caches
.turbo

# Claude Code tooling (local agent assets, not product code)
.claude
skills-lock.json

# Env files — hold per-environment config (and later real API URLs/secrets).
# Managed on the host (Vercel dashboard), NOT committed. Keep .env.example only.
.env
.env.*
!.env.example

# Vercel CLI
.vercel

# Secrets / credentials (defensive — never commit these)
*.pem
*.key
*.p12
*.pfx
id_rsa*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
